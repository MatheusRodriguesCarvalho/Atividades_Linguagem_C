#include <stdio.h>
#include <stdlib.h>

/*
sizeof( "nome da variavel ou typedef struct" ): retorna o tamanho, em bytes, de qualquer variavel

fwrite(
      enderecodo "objeto",
      quantidade de byte vulgo sizeof(),
      quantidade de "objetos" a serem copiados,
      ponteiro do arquivo
)

fread(
      enderecodo "objeto",
      quantidade de byte vulgo sizeof(),
      quantidade de "objetos" a serem copiados,
      ponteiro do arquivo
)

*/

int main()
{
    printf("Hello world!\n");
    return 0;
}
